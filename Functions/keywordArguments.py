def increment(number, by):
    return number + by


print(increment(number=2, by=5)) # It will temporary store it in a variable and then give it as an argument to print fn
# these are keywordArg

    

